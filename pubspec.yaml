name: dartify_plus
description: Dartify Plus — Essential helpers to streamline your Dart and Flutter code.

version: 0.0.2-alpha.2+2
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.6.1

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  intl: ^0.19.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
